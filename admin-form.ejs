<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title><%= typeof product !== 'undefined' ? 'Editar' : 'Adicionar' %> Produto</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #003366; }
        form { max-width: 600px; }
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; }
        input { width: 100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px; box-sizing: border-box; }
        button { background-color: #007bff; color: white; padding: 10px 15px; border: none; border-radius: 4px; cursor: pointer; }
        a { color: #007bff; }
    </style>
</head>
<body>
    <h1><%= typeof product !== 'undefined' ? 'Editar Produto' : 'Adicionar Novo Produto' %></h1>
    
    <form action="<%= typeof product !== 'undefined' ? '/admin/edit/' + product.id : '/admin/add' %>" method="POST">
        <div class="form-group">
            <label for="id">ID (Número único)</label>
            <input type="number" id="id" name="id" value="<%= typeof product !== 'undefined' ? product.id : '' %>" required>
        </div>
        <div class="form-group">
            <label for="nome">Nome do Produto</label>
            <input type="text" id="nome" name="nome" value="<%= typeof product !== 'undefined' ? product.nome : '' %>" required>
        </div>
        <div class="form-group">
            <label for="preco">Preço (Ex: 29,99)</label>
            <input type="text" id="preco" name="preco" value="<%= typeof product !== 'undefined' ? product.preco : '' %>" required>
        </div>
        <div class="form-group">
            <label for="categoria">Categoria (Ex: Roupas, Acessorios)</label>
            <input type="text" id="categoria" name="categoria" value="<%= typeof product !== 'undefined' ? product.categoria : '' %>" required>
        </div>
        
        <button type="submit">Salvar Produto</button>
    </form>

    <br>
    <a href="/admin/dashboard">Voltar para o Dashboard</a>
</body>
</html>