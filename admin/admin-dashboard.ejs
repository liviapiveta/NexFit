<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - NexFit</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background-color: #f9f9f9; }
        h1 { color: #003366; }
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }
        .btn-add { background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px; font-weight: bold; }
        .btn-logout { background-color: #6c757d; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px; margin-left: 10px; }
        table { width: 100%; border-collapse: collapse; background: white; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }
        th, td { border-bottom: 1px solid #ddd; padding: 12px 15px; text-align: left; }
        th { background-color: #003366; color: white; }
        tr:hover { background-color: #f1f1f1; }
        .actions form, .actions a { display: inline-block; margin-right: 5px; }
        .btn-edit { background-color: #ffc107; color: black; padding: 5px 10px; text-decoration: none; border-radius: 3px; font-size: 0.9em; }
        .btn-delete { background-color: #dc3545; color: white; padding: 5px 10px; border: none; border-radius: 3px; cursor: pointer; font-size: 0.9em; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Dashboard de Produtos</h1>
        <div>
            <a href="/" target="_blank" style="margin-right: 15px; text-decoration: none; color: #003366;">Ver Site</a>
            <a href="/admin/add" class="btn-add">Adicionar Produto</a>
            <a href="/admin/logout" class="btn-logout">Sair</a>
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Preço</th>
                <th>Categoria</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <% if (products.length > 0) { %>
                <% products.forEach(product => { %>
                    <tr>
                        <td><%= product.id %></td>
                        <td><%= product.nome %></td>
                        <td>R$<%= product.preco %></td>
                        <td><%= product.categoria %></td>
                        <td class="actions">
                            <a href="/admin/edit/<%= product.id %>" class="btn-edit">Editar</a>
                            <form action="/admin/delete/<%= product.id %>" method="POST" onsubmit="return confirm('Tem certeza que deseja deletar este produto?');">
                                <button type="submit" class="btn-delete">Deletar</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="5" style="text-align: center;">Nenhum produto encontrado.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>